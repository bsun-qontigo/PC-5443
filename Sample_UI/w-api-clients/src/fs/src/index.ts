import { trySetIdentity, trySetUserVars, trySetVars, beginFS } from './internals';
import { sendFsEvent as tryEvent } from './sendEvent';
export {
	tryEvent,
	trySetIdentity,
	trySetUserVars,
	trySetVars,
	beginFS,
};